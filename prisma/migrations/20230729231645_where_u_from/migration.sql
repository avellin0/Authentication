-- CreateTable
CREATE TABLE "local" (
    "id" TEXT NOT NULL,
    "country" TEXT NOT NULL,
    "city" TEXT NOT NULL,
    "neighborhood" TEXT NOT NULL,

    CONSTRAINT "local_pkey" PRIMARY KEY ("id")
);
